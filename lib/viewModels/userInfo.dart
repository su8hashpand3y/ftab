class UserInfo {
  String userId;
  String userName;
  String userImage;
  UserInfo(userId, userName, userImage) {
    this.userName = userName;
    this.userId = userId;
    this.userImage = userImage;
  }
}
